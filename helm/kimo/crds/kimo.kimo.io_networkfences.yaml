---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: networkfences.kimo.kimo.io
spec:
  group: kimo.kimo.io
  names:
    kind: NetworkFence
    listKind: NetworkFenceList
    plural: networkfences
    singular: networkfence
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceRef
      name: Instance
      type: string
    - jsonPath: .status.applied
      name: Applied
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkFence is the Schema for the networkfences API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NetworkFence
            properties:
              allow:
                items:
                  description: AllowRule permits egress to a CIDR/port.
                  properties:
                    cidr:
                      type: string
                    port:
                      format: int32
                      type: integer
                  type: object
                type: array
              allowEgress:
                type: boolean
              deny:
                items:
                  description: DenyRule blocks traffic to a namespace.
                  properties:
                    to:
                      type: string
                  type: object
                type: array
              instanceRef:
                type: string
            required:
            - instanceRef
            type: object
          status:
            description: status defines the observed state of NetworkFence
            properties:
              applied:
                type: boolean
              message:
                type: string
            required:
            - applied
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
