{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kimo.serviceAccountName" . }}
  labels:
    {{- include "kimo.labels" . | nindent 4 }}
{{- end }}
