---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: challengesets.kimo.kimo.io
spec:
  group: kimo.kimo.io
  names:
    kind: ChallengeSet
    listKind: ChallengeSetList
    plural: challengesets
    singular: challengeset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.active
      name: Active
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChallengeSet is the Schema for the challengesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ChallengeSet
            properties:
              challenges:
                items:
                  type: string
                type: array
              schedule:
                description: ScheduleSpec bounds when a ChallengeSet is active.
                properties:
                  endAt:
                    type: string
                  startAt:
                    type: string
                type: object
            required:
            - challenges
            type: object
          status:
            description: status defines the observed state of ChallengeSet
            properties:
              active:
                type: boolean
              message:
                type: string
            required:
            - active
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
