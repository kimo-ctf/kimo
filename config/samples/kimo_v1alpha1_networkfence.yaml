apiVersion: kimo.kimo.io/v1alpha1
kind: NetworkFence
metadata:
  labels:
    app.kubernetes.io/name: workspace
    app.kubernetes.io/managed-by: kustomize
  name: networkfence-sample
spec:
  # TODO(user): Add fields here
